apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "beauty-back.fullname" . }}-test-connection"
  labels:
    {{- include "beauty-back.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "beauty-back.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
