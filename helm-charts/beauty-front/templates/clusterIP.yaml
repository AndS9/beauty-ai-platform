apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-clusterip
  namespace: {{ .Values.common.namespace }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Chart.Name  }}
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080